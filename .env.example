# Copy to .env for local (non-docker) runs. Docker Compose sets these
# itself in docker-compose.yml and does not read this file.
URLSHORT_DATABASE_URL=sqlite:///./urlshort.db
URLSHORT_BASE_URL=http://localhost:8000
URLSHORT_CODE_LENGTH=7
